<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for?(:title) ? yield(:title) : "GreenPal - Lawn Mower Repair" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="description" content="Find the best lawn mower repair shops near you">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
    <link rel="stylesheet" href="https://unpkg.com/leaflet@1.9.4/dist/leaflet.css" />
  </head>
  <body class="bg-gray-50 min-h-screen">
    <%= render 'shared/header' %>
    
    <% if notice %>
      <div class="max-w-7xl mx-auto px-6 py-4">
        <div class="bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded">
          <%= notice %>
        </div>
      </div>
    <% end %>
    
    <% if alert %>
      <div class="max-w-7xl mx-auto px-6 py-4">
        <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded">
          <%= alert %>
        </div>
      </div>
    <% end %>
    
    <main>
      <%= yield %>
    </main>
    
    <%= render 'shared/footer' %>

    <!-- Sticky Bottom Banner -->
    <div class="fixed bottom-0 left-0 right-0 bg-gray-800 text-white py-3 z-50">
      <div class="max-w-7xl mx-auto px-6 flex items-center justify-center gap-4">
        <span>Get <strong>free quotes</strong> for lawn mowing.</span>
        <%= link_to "Get Free Quotes >", get_quotes_path, class: "bg-green-600 hover:bg-green-700 text-white px-4 py-2 rounded-lg text-sm font-semibold transition" %>
      </div>
    </div>
  </body>
</html>
